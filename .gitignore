.DS_Store
Thumbs.db
node_modules/
.dev/
/.check_out.txt

# Local editor / IDE / AI assistant metadata — local to the dev machine,
# not part of the public repo.
.vscode/
.idea/
.claude/
.github/copilot-instructions.md

**/graphify-out/
.graphify*
.graphifyignore
# redirected output of scripts/check.js (logs of previous runs)
/scripts/check.out

# Transient scratch files from local development sessions (not part of
# the published repo). The 'CLAUDE.md.orig.bak' file is a one-off
# backup of CLAUDE.md before a rewrite; '_*.txt' / '_*.log' are
# redirected stdout from ad-hoc terminal commands.
/CLAUDE.md.orig.bak
/_*.txt
/_*.log

# Secrets and credentials — never commit
.env
.env.*
*.pem
*.p12
*.pfx
*.key
*.keystore
id_rsa
id_rsa.pub
id_ed25519
id_ed25519.pub
**/credentials.json
**/service-account*.json
